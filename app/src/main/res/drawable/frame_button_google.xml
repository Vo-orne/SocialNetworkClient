<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="8dp" />

    <stroke
        android:width="2dp"
        android:alpha="0"
        android:color="@color/second_background_color" />

    <gradient
        android:angle="90"
        android:endColor="@color/second_background_color"
        android:startColor="@color/second_background_color" />
</shape>